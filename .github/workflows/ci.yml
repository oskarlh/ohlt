# This starter workflow is for a CMake project running on multiple platforms. There is a different starter workflow if you just want a single platform.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-single-platform.yml
name: CMake on multiple platforms

on:
  pull_request:
  push:

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      # Set fail-fast to false to ensure that feedback is delivered for all matrix combinations. Consider changing this to true when your workflow is stable.
      fail-fast: false

      # Set up a matrix to run the following 3 configurations:
      # 1. <Windows, Release, latest MSVC compiler toolchain on the default runner image, default generator>
      # 2. <Linux, Release, latest GCC compiler toolchain on the default runner image, default generator>
      # 3. <Linux, Release, latest Clang compiler toolchain on the default runner image, default generator>
      #
      # To add more build types (Release, Debug, RelWithDebInfo, etc.) customize the build_type list.
      matrix:
        os: [
          # No Windows support yet...
          # windows-latest,
          macos-15,
          ubuntu-latest
        ]
        build_type: [Release]
        c_compiler: [
          # No Windows support yet...
          # cl,
          clang,
          gcc-14]
        include:
          - os: macos-15
            c_compiler: clang
            cpp_compiler: clang++
          - os: ubuntu-latest
            c_compiler: gcc-14
            cpp_compiler: g++-14
          # No Windows support yet...
          # - os: windows-latest
          #   c_compiler: cl
          #   cpp_compiler: cl
        exclude:
          # No Windows support yet...
          # - os: macos-15
          #   c_compiler: cl
          - os: macos-15
            c_compiler: gcc-14
          # No Windows support yet...
          # - os: ubuntu-latest
          #   c_compiler: cl
          - os: ubuntu-latest
            c_compiler: clang
          # No Windows support yet...
          # - os: windows-latest
          #   c_compiler: clang
          # - os: windows-latest
          #   c_compiler: gcc-14

    steps:
    - uses: actions/checkout@v3

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build/release" >> "$GITHUB_OUTPUT"

    - name: Create output directory
      run: |
        mkdir -p ${{ steps.strings.outputs.build-output-dir }}

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }}
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }}
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
        -S ${{ github.workspace }}
        --preset=release-config

    - name: Build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }} --preset=release-build

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest --build-config ${{ matrix.build_type }}

    - name: Archiving
      # Default Build Artifact zipper does not retain file permission.
      run: 7z a OHLT-${{ matrix.os }}-${{ matrix.c_compiler }}.7z tools

    - name: Upload a Build Artifact
      uses: actions/upload-artifact@v4.3.1
      with:
        name: OHLT-${{ matrix.os }}-${{ matrix.c_compiler }}
        path: OHLT-${{ matrix.os }}-${{ matrix.c_compiler }}.7z
