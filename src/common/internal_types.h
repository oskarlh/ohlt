#pragma once

#include <cstdint>

// This file contains types and contants internal to the compilers, not
// defined by file formats or game engine limitations

using detail_level = std::uint16_t;
using coplanar_priority = std::int16_t;
